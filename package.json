{
  "name": "test-aws-dependencies",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/node": "24.10.1",
    "typescript": "5.9.3",
    "graphviz-cli": "2.0.0"
  },
  "scripts": {
    "amplify":          "cd amplify            && npm run clean_install",
    "amplify-ui":       "cd amplify-ui         && npm run clean_install",
    "amplify-ui-sdk":   "cd amplify-ui-sdk     && npm run clean_install",
    "amplify-ui-sdk-2": "cd amplify-ui-sdk-2   && npm run clean_install",

    "clean_install_all": "npm run amplify && npm run amplify-ui && npm run amplify-ui-sdk && npm run amplify-ui-sdk-2",
        
    "diff": "code --diff '../ai4plan/client/package.json' 'ai4plan-client/package.json'"
  }
}


