# final2

Same as final but we add the following packages that are used only in ai4plan tests

- '@aws-sdk/client-sts'
- '@aws-sdk/credential-providers'
- '@aws-sdk/client-cognito-identity-provider'

